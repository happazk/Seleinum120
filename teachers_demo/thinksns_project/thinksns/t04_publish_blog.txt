定义访问权限和评论权限的列表
privacy_list = ["任何人可见","仅好友可见","私密日记","凭密码访问"]
cc_list = ["任何人可评论","好友可评论","关闭评论"]
privacy = privacy_list[0]
cc = cc_list[0]
循环进行测试
for privacy in privacy_list:
  for cc in cc_list:
     ....

***************发表日志***************
调用登录方法，登录主账号
点击 发表
定义随机文本内容（时间戳）
定义日志标题：访问权限+评论权限+时间戳
输入日志标题 title
先定位出日志内容所在的子页面iframe元素 class ke-iframe
然后切换到该子页面中
sleep(1)
输入日志的内容 class ke-content
切回默认页面
sleep(1)
点击表情图片所在的span元素 class ke-icon-emoticons
先定位出图片所在的div元素，再定位出其范围内所有的img元素 class ke-menu
再随机选择出一个img元素
根据img元素取出其src属性值
再点击该img元素
定义附件所在的路径
上传该附件 myfile
先定位出访问权限所在的下拉框元素，再将其交给Select类进行实例化，再根据可见文本的方法进行选择 privacy
如果访问权限为 凭密码访问 则：
	sleep(1)
	输入密码 password
先定位出评论权限所在的下拉框元素，再将其交给Select类进行实例化，再根据可见文本的方法进行选择 cc
点击发表按钮 //input[@value='发 表']

***************检查日志详情***************
先定位出日志内容所在的div元素 class LogList
根据div元素定位出其范围内的日志标题所在的strong元素，并取出文本值进行判断 //div[@class='tit_log']/h1/strong
根据div元素定位出其范围内的日志内容所在的div元素 id blog_con
根据该div元素，取出其文本值并进行判断
根据该div元素，定位出其下一级的img元素，并取出src属性值进行判断


***************检查访问权限***************
点击 退出
调用登录方法，切换账号
点击 随便看看
点击 日志标题

如果访问权限为 任何人可见 则：
	检查日志详情

如果访问权限为 仅好友可见 则：
	如果 是好友 则：
		检查日志详情
	否则：
		检查访问提示：只有主人的好友可以查看此日志

如果访问权限为 私密日记 则：
	检查访问提示：只有主人可以查看此日志

否则：
	try:
		检查访问提示：本日志需要密码才能访问
		输入密码 password
		点击提交 class btn_b
	except Exception as e:
		print("出现了已知Bug，为了练习顺利进行，所以进行异常处理！但现实企业工作时不能这样做！")
	检查日志详情

***************检查访问提示***************
定位提示所在的div元素，并取出其文本值 class bg_msg_btm
判断期望提示在其中即可


***************检查评论权限***************
如果评论权限为 任何人可评论 则：
	新建评论

如果评论权限为 好友可评论 则：
	如果 是好友 则：
		新建评论
	否则：
		检查评论权限提示：您无法评论,日志发布者设置好友可评论

否则：
	检查评论权限提示：您无法评论,日志发布者已经关闭评论


***************新建评论***************
定义随机评论内容
输入评论内容 textarea
点击发表评论 id content_submit
sleep(1)
先定位出所有的评论记录所在的li元素，并取出第一个 class comlist
根据li元素，定位出其范围内的评论者名字显示所在的a元素，并取出文本值进行判断 //h3[@class='tit_Critique lh25 mb5']/a
根据li元素定位出其范围内评论内容所在的p元素，并取出文本值进行判断

***************检查评论权限提示***************
定位提示所在的div元素，并取出其文本值 class LogList
判断期望提示在其中即可




